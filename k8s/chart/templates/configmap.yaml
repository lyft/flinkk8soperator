kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "flink-operator.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "flink-operator.fullname" . }}-config
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
    app.kubernetes.io/managed-by: "{{ .Release.Service }}"
    team-owner: {{ .Values.teamOwner }}
data:
  config: |-
    operator:
        limitNamespace: {{ .Release.Namespace }}
        resyncPeriod: {{ .Values.resyncPeriod }}
        workers: {{ .Values.workers }}
        maxErrDuration: {{ .Values.maxErrDuration }}
    logger:
        level: 4